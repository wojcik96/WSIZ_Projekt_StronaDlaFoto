import * as VanilliaTilit from 'vanilla-tilt';

VanilliaTilit.init(document.querySelectorAll('.about-as__cointeiner-of-as'), {
   reverse: false,
   max: 2,
   speed: 1000,
   // axis: 'x',
});

// VanilliaTilit.init(document.querySelectorAll('.gallery__img'), {
//    max: 3,
//    speed: 600
// });